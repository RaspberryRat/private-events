<!DOCTYPE html>
<html>
  <head>
    <title>PrivateEvents</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <div class="container">

      <div class="header">
        <ul>
          <li class="site-title"><%= link_to "What's Happening?", root_path %></li>
          <li class="log-out-btn link-btn">
            <%= button_to "Logout", destroy_user_session_path, method: :delete  %>
          </li>
          <li class="link-btn">
            <%= link_to "Sign up here", new_user_registration_path %>
          </li>
          <li class="link-btn">
            <%= button_to "Sign in", user_session_path %>
          </li>
        </ul>
      </div>

         <div class="flash-message">
          <% flash.each do |name, message| %>
          <div class="<%= name %>"><%= message %></div>
          <% end %>
        </div>

      <div class="content-container">
        <%= yield %>
      </div>

      <div class="footer">
        <ul>
          <li class="git-link"><%= link_to "Smelly Industries", 'https://www.github.com/raspberryrat' %></li>
        </ul>
      </div>

    </div>
  </body>
</html>
